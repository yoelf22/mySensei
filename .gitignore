# mySensei ships its skill in-repo, so override the global ignore of .claude/
!.claude/
!.claude/**

node_modules/
.env
.env.*
*.log
.DS_Store

# Generated lessons are kept in git for history, but local scratch is not:
lessons/.scratch/
